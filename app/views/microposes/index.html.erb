<h1>Listing microposes</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @microposes.each do |micropose| %>
  <tr>
    <td><%= micropose.content %></td>
    <td><%= micropose.user_id %></td>
    <td><%= link_to 'Show', micropose %></td>
    <td><%= link_to 'Edit', edit_micropose_path(micropose) %></td>
    <td><%= link_to 'Destroy', micropose, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Micropose', new_micropose_path %>
